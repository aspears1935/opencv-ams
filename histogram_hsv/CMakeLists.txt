cmake_minimum_required(VERSION 2.8)
project( HSV-Histogram )
find_package( OpenCV 3.0.0 EXACT REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable( HSV-Histogram calcHist_Demo.cpp )
target_link_libraries( HSV-Histogram ${OpenCV_LIBS} )
