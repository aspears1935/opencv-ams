cmake_minimum_required(VERSION 2.8)
project( AdaptiveThresh )
find_package( OpenCV REQUIRED )
add_executable( AdaptiveThresh adaptive_thresh.cpp )
target_link_libraries( AdaptiveThresh ${OpenCV_LIBS} )
