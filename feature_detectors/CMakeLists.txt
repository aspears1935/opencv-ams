cmake_minimum_required(VERSION 2.8)
project( FeatureDetectors )
find_package( OpenCV REQUIRED )
add_executable( FeatureDetectors feature_detectors.cpp )
target_link_libraries( FeatureDetectors ${OpenCV_LIBS} )
